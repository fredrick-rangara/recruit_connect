[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "postgresql"]

[phases.install]
cmds = ["python3 -m pip install -r server/requirements.txt"]

[start]
cmd = "cd server && gunicorn wsgi:app"
