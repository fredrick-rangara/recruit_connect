{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeploy": "python -c \"from app import create_app; from models import db; app = create_app(); app.app_context().push(); db.create_all(); print('Tables created')\"",
    "startCommand": "gunicorn wsgi:app --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}